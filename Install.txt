echo "🔧 Iniciando instalação..." && sudo apt update && echo "✓ Repositórios atualizados" && sudo apt install -y unzip zip wget && echo "✓ Dependências instaladas" && rm -rf pawncc com pawnc-3.10.10-linux.zip* && echo "✓ Diretórios limpos" && wget https://raw.githubusercontent.com/48348484488/VSCODE-Pawno-Compiler-For-Android/main/pawnc-3.10.10-linux.zip && echo "✓ Arquivo baixado" && unzip -o pawnc-3.10.10-linux.zip && echo "✓ Arquivo extraído" && rm -rf pawnc-3.10.10-linux.zip pawnc-3.10.10-linux.zip.* && echo "✓ Arquivos temporários removidos" && chmod +x pawncc/bin/pawncc && echo "✓ Permissões aplicadas" && sudo apt update && sudo apt install -y zip wine64 && echo "✓ Wine64 instalado" && chmod +x pawncc/bin/pawncc && echo "✓ Permissões finais aplicadas" && echo "✅ Instalação concluída!"
